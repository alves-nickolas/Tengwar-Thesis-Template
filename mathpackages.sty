\ProvidesPackage{mathpackages}[2023/06/02 MathPackages]

\RequirePackage{xparse}
\RequirePackage{xpatch}
\RequirePackage[svgnames]{xcolor}
\RequirePackage{amsmath}
\RequirePackage{amssymb}    % Extra symbols
\RequirePackage{textgreek} %greek letters in text mode
\RequirePackage[nointegrals]{wasysym}
\let\earth\oplus
\RequirePackage{amsthm}     % Theorem-like environments
\RequirePackage{thmtools}   % Theorem-like environments, extends amsthm
\RequirePackage{mathtools}  % Fonts and environments for mathematical formulae
\RequirePackage{mathrsfs}   % Script font with \mathscr{}
\everymath
{
    \ifodd\value{page}          % Allow multiline equations
        \allowdisplaybreaks[1]  % to break across two pages:
        \else                   % - Breaks from odd to even pages are allowed,
        \allowdisplaybreaks[4]  %   but discouraged.
    \fi                         % - Breaks from even to odd pages are allowed.
}
\RequirePackage[arrowdel]{physics} % Physics notations
\RequirePackage{tensor} % Physics notations
\RequirePackage{slashed} % Physics notations

% Patching physics for use with Garamond

\DeclareDocumentCommand\vectorarrow{ s m }{\vec{\boldsymbol{#2}}} % starred version is the only one

\DeclareDocumentCommand\va{}{\vectorarrow} % Shorthand for \vectorarrow
\DeclareDocumentCommand\vb{}{\vectorarrow} % arrows always turned on

\DeclareDocumentCommand\vectorunit{ s m }{\boldsymbol{\hat{#2}}} % starred version is the only one
\DeclareDocumentCommand\vu{}{\vectorunit} 

